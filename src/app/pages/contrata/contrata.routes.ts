import { Routes } from '@angular/router';
import { ViewContrata } from '@/pages/contrata/components/view-contrata/view-contrata';

export const CONTRATA_ROUTES: Routes = [
    {
        path: '',
        component: ViewContrata
    }
];
