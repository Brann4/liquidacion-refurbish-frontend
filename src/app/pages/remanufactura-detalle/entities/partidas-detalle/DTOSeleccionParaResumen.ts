export interface DTOSeleccionParaResumen {
  partidaId: number;
  partidaItemId: number | null;
  descripcion: string;
  precioUnitario: number;
  precioTotal: number;
  unidad: string;
  cantidad: number;
}
