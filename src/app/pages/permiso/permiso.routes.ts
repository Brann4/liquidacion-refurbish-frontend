import { Routes } from '@angular/router';
import { PermisoListComponent } from './components/list/permiso-list.component';


export const PERMISO_ROUTES: Routes = [
    {
        path: '',
        component: PermisoListComponent,
    },
];
